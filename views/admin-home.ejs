<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Ella Rises Admin</title>
    <style>
      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }

      body {
        font-family: Arial, Helvetica, sans-serif;
        font-size: 14px;
        background-color: #f3f4f6;
        color: #111827;
        min-height: 100vh;
        display: flex;
        align-items: center;
        justify-content: center;
        padding: 24px;
      }

      .admin-shell {
        max-width: 720px;
        width: 100%;
        background: #ffffff;
        border-radius: 16px;
        padding: 24px 24px 20px;
        box-shadow: 0 18px 40px rgba(15, 23, 42, 0.12);
        border: 1px solid #e5e7eb;
      }

      .admin-header {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-bottom: 16px;
      }

      .admin-title {
        font-size: 18px;
        font-weight: 600;
      }

      .admin-user {
        font-size: 12px;
        color: #6b7280;
      }

      .admin-body {
        font-size: 14px;
        color: #4b5563;
        line-height: 1.7;
      }

      .admin-actions {
        margin-top: 18px;
        display: flex;
        justify-content: space-between;
        align-items: center;
        gap: 8px;
      }

      .admin-link {
        font-size: 12px;
        color: #2563eb;
        text-decoration: none;
      }

      .admin-link:hover {
        text-decoration: underline;
      }

      .admin-logout {
        border: none;
        background: #ef4444;
        color: #ffffff;
        padding: 7px 14px;
        border-radius: 999px;
        font-size: 11px;
        letter-spacing: 0.08em;
        text-transform: uppercase;
        cursor: pointer;
      }

      .admin-logout:hover {
        background: #b91c1c;
      }
    </style>
  </head>
  <body>
    <div class="admin-shell">
      <div class="admin-header">
        <div>
          <h1 class="admin-title">Ella Rises admin</h1>
          <p class="admin-user">
            Signed in as
            <strong><%= user.firstName %> <%= user.lastName %></strong>
            (<%= user.permission %>)
          </p>
        </div>
        <form method="POST" action="/admin/logout">
          <button type="submit" class="admin-logout">Log out</button>
        </form>
      </div>

      <div class="admin-body">
        <p>
          This is your admin home. From here, you&rsquo;ll eventually be able to
          manage uploads, monitor normalization runs, and view internal
          reporting tools.
        </p>
        <p style="margin-top: 8px;">
          For now, this page simply confirms that login and permissions are
          working correctly.
        </p>
      </div>

      <div class="admin-actions">
        <a href="/dashboard" class="admin-link">View public impact dashboard</a>
        <a href="/" class="admin-link">Back to Ella Rises home</a>
      </div>
    </div>
  </body>
</html>
