<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Request Ella Rises Admin Access</title>

    <style>
      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }

      body {
        font-family: Arial, Helvetica, sans-serif;
        font-size: 14px;
        background-color: #ffece4;
        color: #1f1b1a;
        min-height: 100vh;
        display: flex;
        align-items: flex-start;
        justify-content: center;
        position: relative;
        overflow-x: hidden;
        overflow-y: auto;
      }

      .request-shell {
        max-width: 600px;
        width: 100%;
        padding: 28px 16px 52px;
        position: relative;
        z-index: 1;
      }

      .request-header {
        text-align: center;
        margin-bottom: 32px;
      }

      .request-logo {
        display: flex;
        align-items: center;
        justify-content: center;
        margin-bottom: 24px;
      }

      .request-logo img {
        width: 60px;
        height: 60px;
        border-radius: 999px;
        object-fit: cover;
        transform: scale(1.6);
        transform-origin: center;
      }

      .request-eyebrow {
        font-size: 15px;
        letter-spacing: 0.18em;
        text-transform: uppercase;
        color: #8c6a61;
        margin-bottom: 16px;
        text-align: center;
      }

      .request-title {
        font-size: 50px;
        line-height: 1.35;
        color: #231815;
        text-align: center;
        margin-bottom: 24px;
      }

      .request-subtitle {
        font-size: 17px;
        line-height: 1.9;
        color: #5a4b47;
        max-width: 460px;
        margin: 0 auto 32px;
        text-align: center;
      }

      .request-card {
        max-width: 520px;
        margin: 0 auto;
        background: rgba(255, 255, 255, 0.85);
        border-radius: 20px;
        padding: 22px 22px 20px;
        box-shadow: 0 10px 22px rgba(0, 0, 0, 0.1);
      }

      .request-field {
        margin-bottom: 14px;
      }

      .request-label {
        display: block;
        font-size: 15px;
        font-weight: 600;
        color: #4b4b4b;
        margin-bottom: 4px;
      }

      .request-input,
      .request-textarea,
      .request-password {
        width: 100%;
        padding: 10px 12px;
        border-radius: 10px;
        border: 1px solid #d1d5db;
        font-size: 15px;
        background: #f9fafb;
        color: #5a4b47;
      }

      .request-textarea {
        min-height: 80px;
        resize: vertical;
        font-family: inherit;
        line-height: 1.8;
      }

      .request-input:focus,
      .request-textarea:focus,
      .request-password:focus {
        outline: none;
        border-color: #7db0d6;
        box-shadow: 0 0 0 1px #7db0d6;
        background: #ffffff;
      }

      .request-actions {
        margin-top: 12px;
        display: flex;
        align-items: center;
        justify-content: space-between;
        gap: 12px;
        flex-wrap: wrap;
      }

      .request-submit {
        padding: 12px 28px;
        border-radius: 999px;
        border: none;
        background: #2f2623;
        color: #ffffff;
        font-size: 14px;
        letter-spacing: 0.08em;
        text-transform: uppercase;
        cursor: pointer;
        box-shadow: 0 6px 14px rgba(0, 0, 0, 0.14);
      }

      .request-submit:hover {
        background: #1f1714;
      }

      .request-back-link {
        font-size: 12px;
        color: #6b5a56;
        text-decoration: none;
      }

      .request-back-link:hover {
        text-decoration: underline;
      }

      .request-message {
        font-size: 13px;
        margin-bottom: 12px;
      }

      .request-message--error {
        color: #b91c1c;
      }

      .request-message--success {
        color: #166534;
      }

      .request-required-note {
        font-size: 12px;
        color: #6b5a56;
        margin-bottom: 10px;
      }

      .request-label-required::after {
        content: ' *';
        color: #b91c1c;
        font-weight: 600;
      }

      .bg-bubble {
        position: absolute;
        border-radius: 999px;
        background-color: rgba(255, 255, 255, 0.26);
        pointer-events: none;
        mix-blend-mode: normal;
        animation-name: bubble-drift;
        animation-timing-function: ease-in-out;
        animation-iteration-count: infinite;
        animation-direction: alternate;
        z-index: 0;
      }

      .bg-bubble--peach {
        background-color: rgba(255, 194, 166, 0.32);
      }

      .bg-bubble--lilac {
        background-color: rgba(216, 205, 246, 0.36);
      }

      .bg-bubble--sage {
        background-color: rgba(204, 221, 205, 0.36);
      }

      .bg-bubble--tl {
        width: 120px;
        height: 120px;
        top: -40px;
        left: -40px;
        animation-duration: 22s;
        animation-delay: -6s;
      }

      .bg-bubble--tr {
        width: 110px;
        height: 110px;
        top: 40px;
        right: -30px;
        animation-duration: 24s;
        animation-delay: -10s;
      }

      .bg-bubble--bl {
        width: 100px;
        height: 100px;
        bottom: -40px;
        left: 18%;
        animation-duration: 26s;
        animation-delay: -14s;
      }

      .bg-bubble--br {
        width: 140px;
        height: 140px;
        bottom: -60px;
        right: 8%;
        animation-duration: 24s;
        animation-delay: -4s;
      }

      .bg-bubble--mid-1 {
        width: 70px;
        height: 70px;
        top: 52%;
        left: 8%;
        animation-duration: 20s;
        animation-delay: -3s;
      }

      .bg-bubble--mid-2 {
        width: 80px;
        height: 80px;
        top: 46%;
        right: 10%;
        animation-duration: 21s;
        animation-delay: -9s;
      }

      .bg-bubble--mid-3 {
        width: 60px;
        height: 60px;
        top: 30%;
        left: 55%;
        animation-duration: 19s;
        animation-delay: -6s;
      }

      @keyframes bubble-drift {
        0% {
          transform: translate3d(0, 0, 0) scale(1);
        }
        50% {
          transform: translate3d(12px, -8px, 0) scale(1.04);
        }
        100% {
          transform: translate3d(-8px, 6px, 0) scale(0.98);
        }
      }

      /* Make layout breathe on smaller screens without cutting off the card */
      @media (max-width: 600px) {
        .request-title {
          font-size: 32px;
        }

        .request-subtitle {
          font-size: 15px;
          margin-bottom: 24px;
        }

        .request-shell {
          padding: 40px 16px;
        }

        .request-card {
          padding: 18px 16px 18px;
        }
      }

      /* On short viewports, reduce vertical padding/scale so the card stays inside
         the viewport without feeling smashed to the edges. */
      @media (max-height: 720px) {
        body {
          align-items: flex-start;
        }

        .request-shell {
          padding-top: 32px;
          padding-bottom: 32px;
        }

        .request-title {
          font-size: 36px;
          margin-bottom: 20px;
        }

        .request-subtitle {
          font-size: 15px;
          margin-bottom: 24px;
        }

        .request-card {
          padding: 18px 18px 18px;
        }
      }

      @media (max-height: 620px) {
        .request-title {
          font-size: 30px;
        }

        .request-subtitle {
          font-size: 14px;
        }

        .request-shell {
          padding-top: 24px;
          padding-bottom: 24px;
        }
      }
    </style>
  </head>
  <body>
    <div class="bg-bubble bg-bubble--peach bg-bubble--tl"></div>
    <div class="bg-bubble bg-bubble--lilac bg-bubble--tr"></div>
    <div class="bg-bubble bg-bubble--sage bg-bubble--bl"></div>
    <div class="bg-bubble bg-bubble--peach bg-bubble--br"></div>
    <div class="bg-bubble bg-bubble--lilac bg-bubble--mid-1"></div>
    <div class="bg-bubble bg-bubble--sage bg-bubble--mid-2"></div>
    <div class="bg-bubble bg-bubble--peach bg-bubble--mid-3"></div>

    <div class="request-shell">
      <div class="request-header">
        <div class="request-logo">
          <img src="/images/ella-rises-logo.png" alt="Ella Rises" />
        </div>
        <div class="request-eyebrow">Ella Rises Admin</div>
        <h1 class="request-title">Request access to the admin corner</h1>
        <p class="request-subtitle">
          Share a bit about yourself and how you plan to use the Ella Rises
          dashboards and tools. Your request will be reviewed by a member of our
          team.
        </p>
      </div>

      <div class="request-card">
        <% if (error) { %>
        <div class="request-message request-message--error"><%= error %></div>
        <% } %>

        <% if (success) { %>
        <div class="request-message request-message--success"><%= success %></div>
        <% } %>

        <div class="request-required-note">Fields marked with <span style="color:#b91c1c;">*</span> are required.</div>

        <form method="POST" action="/admin/create">
          <div class="request-field">
            <label class="request-label request-label-required" for="email">Work email</label>
            <input
              type="email"
              id="email"
              name="email"
              class="request-input"
              required
              value="<%= form && form.email ? form.email : '' %>"
            />
          </div>

          <div class="request-field">
            <label class="request-label request-label-required" for="first_name">First name</label>
            <input
              type="text"
              id="first_name"
              name="first_name"
              class="request-input"
              required
              value="<%= form && form.first_name ? form.first_name : '' %>"
            />
          </div>

          <div class="request-field">
            <label class="request-label request-label-required" for="last_name">Last name</label>
            <input
              type="text"
              id="last_name"
              name="last_name"
              class="request-input"
              required
              value="<%= form && form.last_name ? form.last_name : '' %>"
            />
          </div>

          <div class="request-field">
            <label class="request-label" for="organization">Organization</label>
            <input
              type="text"
              id="organization"
              name="organization"
              class="request-input"
              value="<%= form && form.organization ? form.organization : '' %>"
            />
          </div>

          <div class="request-field">
            <label class="request-label" for="message">
              How are you planning to use Ella Rises data and dashboards?
            </label>
            <textarea
              id="message"
              name="message"
              class="request-textarea"
            ><%= form && form.message ? form.message : '' %></textarea>
          </div>

          <div class="request-field">
            <label class="request-label request-label-required" for="password">Create a password</label>
            <input
              type="password"
              id="password"
              name="password"
              class="request-password"
              required
            />
          </div>

          <div class="request-actions">
            <button type="submit" class="request-submit">Submit request</button>
            <a href="/admin" class="request-back-link">Back to admin gateway</a>
          </div>
        </form>
      </div>
    </div>
  </body>
</html>
