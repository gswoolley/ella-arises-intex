<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Ella Rises Admin Login</title>
    <style>
      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }

      body {
        font-family: Arial, Helvetica, sans-serif;
        font-size: 14px;
        background-color: #ffece4;
        color: #1f1b1a;
        min-height: 100vh;
        display: flex;
        align-items: flex-start;
        justify-content: center;
        position: relative;
        overflow: hidden;
      }

      .login-shell {
        max-width: 600px;
        width: 100%;
        text-align: center;
        padding: 32px 16px 56px;
        position: relative;
        z-index: 1;
      }

    .login-logo {
      display: flex;
      align-items: center;
      justify-content: center;
      margin: 0 auto 32px; /* center in shell + space below */
      width: 80px;
      height: 80px;
      border-radius: 999px;
      overflow: hidden;
      background-color: #ffece4; /* same as page bg */
      box-shadow: none;
    }

    .login-logo img {
      width: 100%;
      height: 100%;
      object-fit: contain; /* show full circle logo */
      transform: none;
    }

      .login-eyebrow {
        font-size: 15px;
        letter-spacing: 0.18em;
        text-transform: uppercase;
        color: #8c6a61;
        margin-bottom: 16px;
      }

      .login-title {
        font-size: 50px;
        line-height: 1.3;
        color: #231815;
        margin-bottom: 24px;
      }

      .login-subtitle {
        font-size: 17px;
        line-height: 1.9;
        color: #5a4b47;
        max-width: 460px;
        margin: 0 auto 32px;
      }

      .login-card {
        max-width: 420px;
        margin: 0 auto;
        background: #ffffff;
        border-radius: 20px;
        padding: 22px 22px 20px;
        box-shadow: 0 10px 22px rgba(0, 0, 0, 0.1);
        border: 1px solid rgba(0, 0, 0, 0.04);
        text-align: left;
      }

      .login-field {
        margin-bottom: 14px;
      }

      .login-label {
        display: block;
        font-size: 15px;
        font-weight: 600;
        color: #4b4b4b;
        margin-bottom: 4px;
      }

      .login-input {
        width: 100%;
        padding: 10px 12px;
        border-radius: 10px;
        border: 1px solid #d1d5db;
        font-size: 17px;
        background: #f9fafb;
      }

      .login-input:focus {
        outline: none;
        border-color: #7db0d6;
        box-shadow: 0 0 0 1px #7db0d6;
        background: #ffffff;
      }

      .login-error {
        background: #fef2f2;
        border: 1px solid #fecaca;
        color: #b91c1c;
        border-radius: 10px;
        padding: 8px 10px;
        font-size: 12px;
        margin-bottom: 14px;
      }

      .login-actions {
        margin-top: 12px;
        display: flex;
        justify-content: space-between;
        align-items: center;
        gap: 8px;
      }

      .login-submit {
        display: inline-flex;
        align-items: center;
        justify-content: center;
        padding: 12px 30px;
        border-radius: 999px;
        border: none;
        background: #2f2623;
        color: #ffffff;
        font-size: 14px;
        letter-spacing: 0.08em;
        text-transform: uppercase;
        cursor: pointer;
        box-shadow: 0 6px 14px rgba(0, 0, 0, 0.14);
      }

      .login-submit:hover {
        background: #1f1714;
      }

      .login-link {
        font-size: 12px;
        color: #6b5a56;
        text-decoration: none;
      }

      .login-link:hover {
        text-decoration: underline;
      }

      .bg-bubble {
        position: absolute;
        border-radius: 999px;
        background-color: rgba(255, 255, 255, 0.26);
        pointer-events: none;
        mix-blend-mode: normal;
        animation-name: bubble-drift;
        animation-timing-function: ease-in-out;
        animation-iteration-count: infinite;
        animation-direction: alternate;
        z-index: 0;
      }

      .bg-bubble--peach {
        background-color: rgba(255, 194, 166, 0.32);
      }

      .bg-bubble--lilac {
        background-color: rgba(216, 205, 246, 0.36);
      }

      .bg-bubble--sage {
        background-color: rgba(204, 221, 205, 0.36);
      }

      .bg-bubble--tl {
        width: 120px;
        height: 120px;
        top: -40px;
        left: -40px;
        animation-duration: 22s;
        animation-delay: -6s;
      }

      .bg-bubble--tr {
        width: 110px;
        height: 110px;
        top: 40px;
        right: -30px;
        animation-duration: 24s;
        animation-delay: -10s;
      }

      .bg-bubble--bl {
        width: 100px;
        height: 100px;
        bottom: -40px;
        left: 18%;
        animation-duration: 26s;
        animation-delay: -14s;
      }

      .bg-bubble--br {
        width: 140px;
        height: 140px;
        bottom: -60px;
        right: 8%;
        animation-duration: 24s;
        animation-delay: -4s;
      }

      /* mid-field bubbles for subtle motion nearer the content */
      .bg-bubble--mid-1 {
        width: 70px;
        height: 70px;
        top: 52%;
        left: 8%;
        animation-duration: 20s;
        animation-delay: -3s;
      }

      .bg-bubble--mid-2 {
        width: 80px;
        height: 80px;
        top: 46%;
        right: 10%;
        animation-duration: 21s;
        animation-delay: -9s;
      }

      .bg-bubble--mid-3 {
        width: 60px;
        height: 60px;
        top: 30%;
        left: 55%;
        animation-duration: 19s;
        animation-delay: -6s;
      }

      @keyframes bubble-drift {
        0% {
          transform: translate3d(0, 0, 0) scale(1);
        }
        50% {
          transform: translate3d(12px, -8px, 0) scale(1.03);
        }
        100% {
          transform: translate3d(-8px, 6px, 0) scale(0.98);
        }
      }

      @media (max-width: 480px) {
        body {
          padding: 16px;
        }

        .login-shell {
          padding: 32px 16px 24px;
        }

        .login-card {
          padding: 16px 14px 14px;
        }
      }
    </style>
  </head>
  <body>
    <div class="bg-bubble bg-bubble--peach bg-bubble--tl"></div>
    <div class="bg-bubble bg-bubble--sage bg-bubble--tr"></div>
    <div class="bg-bubble bg-bubble--lilac bg-bubble--bl"></div>
    <div class="bg-bubble bg-bubble--peach bg-bubble--br"></div>
    <div class="bg-bubble bg-bubble--lilac bg-bubble--mid-1"></div>
    <div class="bg-bubble bg-bubble--sage bg-bubble--mid-2"></div>
    <div class="bg-bubble bg-bubble--peach bg-bubble--mid-3"></div>

    <div class="login-shell">
      <div class="login-logo">
        <img src="/images/ella-rises-logo.png" alt="Ella Rises" />
      </div>

      <div class="login-eyebrow">Admin login</div>
      <h1 class="login-title">Sign in to continue the work.</h1>
      <p class="login-subtitle">
        Use your Ella Rises admin credentials to access internal tools,
        organizational data, and impact dashboards.
      </p>

      <div class="login-card">
        <% if (error) { %>
        <div class="login-error"><%= error %></div>
        <% } %>

        <form method="POST" action="/admin/login">
          <div class="login-field">
            <label for="email" class="login-label">Email</label>
            <input
              id="email"
              name="email"
              type="email"
              class="login-input"
              value="<%= email %>"
              required
            />
          </div>

          <div class="login-field">
            <label for="password" class="login-label">Password</label>
            <input
              id="password"
              name="password"
              type="password"
              class="login-input"
              required
            />
          </div>

          <div class="login-actions">
            <a href="/admin" class="login-link">Back to admin gateway</a>
            <button type="submit" class="login-submit">Sign in</button>
          </div>
        </form>
      </div>
    </div>
  </body>
</html>
